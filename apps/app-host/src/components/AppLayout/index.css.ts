import { style } from "@vanilla-extract/css";

export const container = style({
  width: "100%",
  marginTop: 120,
  marginBottom: 50,
});

export const wrapper = style({
  maxWidth: "1200px",
  margin: "0 auto",
});
